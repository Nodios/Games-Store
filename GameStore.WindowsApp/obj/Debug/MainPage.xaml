<common:PageBase xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
                 xmlns:common="using:GameStore.WindowsApp.Common"
                 xmlns:local="using:GameStore.WindowsApp"
                 xmlns:ignore="http://www.ignore.com"
                 x:Name="PageRoot"
                 x:Class="GameStore.WindowsApp.MainPage"
                 mc:Ignorable="d ignore"
                 DataContext="{Binding Main, Source={StaticResource Locator}}">

    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Skins/MainSkin.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>

    <interactivity:Interaction.Behaviors>
        <common:OrientationStateBehavior PortraitFlippedStateName="Portrait" />
    </interactivity:Interaction.Behaviors>


    <!-- Main layout , guidelines : https://msdn.microsoft.com/en-us/library/windows/apps/hh872191.aspx -->
    <Grid Background="{ThemeResource AppBarBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
    
    
    <!-- Title -->
        <TextBlock Style="{StaticResource HeaderTextBlockStyle}"
                   Text="{Binding Title}"
                   Grid.Row="1"
                   Grid.Column="2"/>
        <StackPanel Grid.Column="2" Grid.Row="2" Orientation="Horizontal">
            <TextBlock Style="{StaticResource SubheaderTextBlockStyle}" Text="Games"/>
            <TextBlock Style="{StaticResource SubheaderTextBlockStyle}" Text="Publishers"/>
            <TextBlock Style="{StaticResource SubheaderTextBlockStyle}" Text="Account"/>
        </StackPanel>
        
    </Grid>
    
</common:PageBase>

